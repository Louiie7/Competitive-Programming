#include <bits/stdc++.h>
using namespace std;
int main() {
    int A;
    int N;
    cin >> A;
    cin >> N;
    vector<int> factors;
    int i = 0;
    while(i < A+1 && i < N)
        if(A % i == 0) {
            factors.push_back(i);
        }
    }
    
    
}
