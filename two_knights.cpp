#include <bits/stdc++.h>
using namespace std;
#define ll long long;
vector<ll> dp;
long lastPos = 0;
int factorial(n){
	for(int 


void knightPos(int n){
	if(n == 1) {
		cout << 0;
	}
	if(n == 2) {
		cout << 6;
	}
	if(n == 3) {
		cout << 28;
	}
	if(n > 3) {
		ll maxPos = :wq
		factorial(4)/(factorial(2)*factorial(n-2))
	}
}
int main(){

	int n;
	cin >> n;
	for(int i = 0; i < n; i++){
		
		knightPos(n);	
	}
}
